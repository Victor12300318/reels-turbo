# Strict rotation per user video cycle

To eliminate repeated Reels visuals, each user has an independent rotation cycle: a video can only be reused after every eligible video has been used once. The AI still picks the best match, but only inside the pool of videos not yet used in that cycle, and the cycle resets automatically when exhausted.
